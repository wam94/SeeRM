# SeeRM configuration example
# Copy this file to one of the following locations and edit values:
#  - Project root: ./.env (works when running from the repo)
#  - User config:  ~/.seerm/.env  (default picked up automatically)
#  - Custom path:  export SEERM_CONFIG=/path/to/your/.env

# ---------------------------------
# Gmail (required for email sending and Gmail-based CSV ingestion)
#
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
GMAIL_USER=

# Optional: Override the Gmail search used to find CSV
# Example default: from:metabase@mercury.com subject:"Alert: SeeRM Master Query has results" has:attachment filename:csv newer_than:10d
GMAIL_QUERY=

# ---------------------------------
# Data source (required for intelligence reports)
# Provide one of:
#  - CSV_SOURCE_PATH: Local CSV file to read
#  - Or rely on Gmail query to fetch the CSV attachment
CSV_SOURCE_PATH=

# ---------------------------------
# Digest recipients (optional; defaults to GMAIL_USER)
DIGEST_TO=
DIGEST_CC=
DIGEST_BCC=

# ---------------------------------
# Notion (optional but recommended)
NOTION_API_KEY=
NOTION_COMPANIES_DB_ID=
NOTION_REPORTS_DB_ID=
NOTION_INTEL_DB_ID=

# Optional: Improve generated Notion links
NOTION_WORKSPACE_NAME=
NOTION_COMPANIES_VIEW_ID=

# ---------------------------------
# Intelligence settings
INTELLIGENCE_REPORTS_ENABLED=true
INTELLIGENCE_DEFAULT_REPORT_DAYS=7
INTELLIGENCE_MAX_NEWS_PER_COMPANY=10
INTELLIGENCE_RISK_ASSESSMENT_ENABLED=true

# Optional: Filter callsigns (comma-separated) if you want to hard-scope a user’s portfolio
# FILTER_CALLSIGNS=ACME,BETA,GAMMA

# ---------------------------------
# AI / enrichment (optional)
OPENAI_API_KEY=
GOOGLE_API_KEY=
GOOGLE_CSE_ID=

# ---------------------------------
# App runtime
ENVIRONMENT=production
DRY_RUN=false
DEBUG=false
MAX_WORKERS=6
REQUEST_TIMEOUT=30
RATE_LIMIT_CALLS_PER_SECOND=2.5
